import "package:unittest/unittest.dart";
import "package:project_pluto/project_pluto.dart";

main() {
  test("can construct a js_variable object", () {
    
  });
  test("can get js_variable name", () {
    
  });
  test("can alter values on js_variable", () {
    
  });
  test("can compare two compatible js_variable objects", () {
    
  });
  test("can not compare two incompatible js_variable objects", () {
    
  });
}